\documentclass[tikz, border=2pt]{standalone}
\usepackage{ctex}
\usepackage{tikz}
\usetikzlibrary{shadows, positioning, arrows.meta, decorations.pathmorphing, fit, backgrounds, calc}
\begin{document}
\begin{tikzpicture}[
        font=\sffamily,
        >=Stealth,
        % Styles
        phase_bg/.style={draw=gray!30, fill=#1, rounded corners=8pt, inner sep=15pt, dashed, thick},
        block/.style={draw=black!70, fill=white, rounded corners=4pt, text width=3.2cm, align=center, minimum height=1.3cm, thick, drop shadow={opacity=0.15}},
        small_block/.style={block, minimum height=0.8cm, text width=2.5cm, fill=white!95!gray},
        highlight_block/.style={block, fill=red!5, draw=red!60, thick},
        final_block/.style={block, fill=green!10, draw=green!60!black, thick, text width=3.5cm},
        arrow/.style={-{Stealth[scale=1.2]}, thick, draw=black!80},
        lbl/.style={fill=white, inner sep=2pt, font=\small, rounded corners=2pt}
    ]

    % Core Data Nodes (Bottom Row)
    \node[block] (survivors) {硬界资质筛查\\($RawScore \ge \theta$)};
    \node[block, left=2.5cm of survivors, fill=blue!5, draw=blue!80] (data_in) {单层计算梯度流\\$\{\Delta W_{k}^{(l)}\}_{k \in \mathcal{A}}$};
    \node[block, right=3.5cm of survivors] (clipped) {动态振幅修剪圈\\$\widehat{\Delta W}_{k}^{(l)}$};
    \node[final_block, right=3.8cm of clipped] (agg) {分层权重聚合总成\\$\Delta W_{global}^{(l)}$};
    
    \draw[arrow] (data_in) -- (survivors);
    \draw[arrow] (survivors) -- node[lbl, above] {出线晋级名单 $\Phi^{(l)}$} (clipped);
    \draw[arrow] (clipped) -- node[lbl, above, align=center] {重分配 $\tilde{w}_k^{(l)}$\\二次合装} (agg);

    % Control Nodes (Intermediate)
    \node[highlight_block, above=2.8cm of survivors] (theta) {生成控制底线\\$\theta^{(l)}$};
    \node[highlight_block, above=2.8cm of clipped] (clip) {约束裁剪半径\\$C^{(l)}$};

    \draw[arrow, dashed, draw=red!80, line width=1.5pt] (theta) -- node[lbl, right] {上提基准限度 $\uparrow$} (survivors);
    \draw[arrow, dashed, draw=red!80, line width=1.5pt] (clip) -- node[lbl, right] {向心收紧边界 $\downarrow$} (clipped);

    % Control Nodes (Top S_total)
    \path (theta) -- node[pos=0.5] (mid_tc) {} (clip);
    \node[block, fill=purple!5, draw=purple!80, above=2.5cm of mid_tc] (stotal) {防卫诉求主控中枢\\$S_{total}^{(l)}$};
    
    \draw[arrow] (stotal) -| (theta);
    \draw[arrow] (stotal) -| (clip);

    % Sensors
    \node[small_block, left=3.5cm of stotal, yshift=1.2cm] (sp) {层级隐私维度 $S_{privacy}$};
    \node[small_block, left=3.5cm of stotal, yshift=0cm] (su) {更新效能维度 $S_{utility}$};
    \node[small_block, left=3.5cm of stotal, yshift=-1.2cm] (ss) {后门安全维度 $S_{security}$};

    \draw[arrow] (sp.east) -- (stotal.west);
    \draw[arrow] (su.east) -- (stotal.west);
    \draw[arrow] (ss.east) -- (stotal.west);
    
    % Grouping Backgrounds
    \begin{scope}[on background layer]
        \node[phase_bg=purple!4, fit=(sp) (ss) (stotal) (theta) (clip)] (bgControl) {};
        \node[font=\bfseries\large, text=purple!80!black, fill=white, inner sep=2pt] at (bgControl.north west) [anchor=south west, xshift=15pt, yshift=-8pt] {自适应层级防线控制反馈中枢 (Control Core)};
        
        \node[phase_bg=orange!4, fit=(data_in) (survivors) (clipped) (agg)] (bgData) {};
        \node[font=\bfseries\large, text=orange!80!black, fill=white, inner sep=2pt] at (bgData.north west) [anchor=south west, xshift=15pt, yshift=-8pt] {深度网格参数剥离与等比降频剪裁流 (Data Pipeline)};
    \end{scope}

    \end{tikzpicture}
\end{document}
