% !TeX program = xelatex
\documentclass[tikz, border=2pt]{standalone}
\usepackage{ctex}
\usepackage{tikz}
\usetikzlibrary{shadows, positioning, arrows.meta, decorations.pathmorphing, fit, backgrounds, calc}
\begin{document}
\begin{tikzpicture}[
        font=\sffamily,
        >=Stealth,
        node distance=3cm and 3.5cm,
        % State definitions
        state_base/.style={circle, draw, very thick, minimum size=3.2cm, align=center, drop shadow={opacity=0.15}},
        normal/.style={state_base, fill=green!10, draw=green!60!black},
        suspect/.style={state_base, fill=orange!10, draw=orange!80!black, dashed},
        quar/.style={state_base, fill=red!10, draw=red!70!black},
        black/.style={state_base, fill=gray!20, draw=black!90, double, double distance=2pt},
        % Edge definitions
        edge_norm/.style={->, thick, draw=blue!60!black},
        edge_warn/.style={->, thick, draw=orange!80!black},
        edge_danger/.style={->, thick, draw=red!80!black},
        edge_fatal/.style={->, ultra thick, draw=black!80, dashed},
        lbl/.style={fill=white, inner sep=4pt, font=\small, rounded corners=2pt, align=center}
    ]

    % Nodes
    \node[normal] (N) {\textbf{NORMAL}\\常规聚合节点\\全权状态};
    \node[suspect, above right=2.5cm and 4.8cm of N] (S) {\textbf{SUSPECT}\\嫌疑监控态\\行为审查};
    \node[quar, below right=2.5cm and 4.8cm of N] (Q) {\textbf{QUARANTINE}\\物理隔离态\\权值剥夺};
    \node[black, right=4.2cm of Q] (B) {\textbf{BLACKLIST}\\绝对黑名单\\不可逆封禁};

    % Edges with specific bending to avoid straight line crossings
    \path
        % N to S and S to N (上半区走廊，与下半区彻底绝缘)
        (N) edge[edge_warn, bend left=15] node[lbl, sloped, above] {瞬时风险评估超限 ($RiskEMA \uparrow$)} (S)
        (S) edge[edge_norm, bend left=15] node[lbl, sloped, below] {长效观测风险回退 (状态特赦)} (N)
        
        % N to Q and Q to N (下半区走廊)
        (N) edge[edge_danger, bend right=15] node[lbl, sloped, below] {历史效用断崖滑坡 ($HistPerf \downarrow$)} (Q)
        (Q) edge[edge_norm, bend right=15] node[lbl, sloped, above] {限制域测试重归合规 (解除隔离)} (N)
        
        % S to Q
        (S) edge[edge_danger, bend left=15] node[lbl, fill=white, inner sep=4pt] {持续异常触发隔离红线} (Q)
        
        % Q to B
        (Q) edge[edge_fatal] node[lbl, above] {命中拒绝服务硬阈值, 物理阻断} (B)
        
        % B loop
        (B) edge[->, ultra thick, draw=black!80, loop right, looseness=5] node[lbl, right] {吊销 TEE 证书\\永久终止握手} (B);
        
    \end{tikzpicture}
\end{document}
