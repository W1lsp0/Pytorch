% !TeX program = xelatex
\documentclass[tikz, border=2pt]{standalone}
\usepackage{ctex}
\usepackage{tikz}
\usetikzlibrary{shadows, positioning, arrows.meta, decorations.pathmorphing, fit, backgrounds, calc}
\begin{document}
\begin{tikzpicture}[
        font=\sffamily,
        >=Stealth,
        node distance=3cm and 3.5cm,
        % State definitions
        state_base/.style={circle, draw, very thick, minimum size=3.2cm, align=center, drop shadow={opacity=0.15}},
        normal/.style={state_base, fill=green!10, draw=green!60!black},
        suspect/.style={state_base, fill=orange!10, draw=orange!80!black, dashed},
        quar/.style={state_base, fill=red!10, draw=red!70!black},
        black/.style={state_base, fill=gray!20, draw=black!90, double, double distance=2pt},
        % Edge definitions
        edge_norm/.style={->, thick, draw=blue!60!black},
        edge_warn/.style={->, thick, draw=orange!80!black},
        edge_danger/.style={->, thick, draw=red!80!black},
        edge_fatal/.style={->, ultra thick, draw=black!80, dashed},
        lbl/.style={fill=white, inner sep=4pt, font=\small, rounded corners=2pt, align=center}
    ]

    % Nodes
    \node[normal] (N) {\textbf{NORMAL}\\Regular Aggregation Node\\Full Privilege State};
    \node[suspect, above right=2.5cm and 4.8cm of N] (S) {\textbf{SUSPECT}\\Suspicious Monitoring State\\Behavioral Review};
    \node[quar, below right=2.5cm and 4.8cm of N] (Q) {\textbf{QUARANTINE}\\Physical Quarantine State\\Weight Deprivation};
    \node[black, right=4.2cm of Q] (B) {\textbf{BLACKLIST}\\Absolute Blacklist\\Irreversible Ban};

    % Edges with specific bending to avoid straight line crossings
    \path
        % N to S and S to N
        (N) edge[edge_warn, bend left=15] node[lbl, sloped, above] {Instant Risk Eval Exceeds Limit ($RiskEMA \uparrow$)} (S)
        (S) edge[edge_norm, bend left=15] node[lbl, sloped, below] {Long-term Observation Risk Recedes} (N)
        
        % N to Q and Q to N
        (N) edge[edge_danger, bend right=15] node[lbl, sloped, below] {Historic Utility Drop ($HistPerf \downarrow$)} (Q)
        (Q) edge[edge_norm, bend right=15] node[lbl, sloped, above] {Restricted Test Passes (Amnesty)} (N)
        
        % S to Q
        (S) edge[edge_danger, bend left=15] node[lbl, fill=white, inner sep=4pt] {Persistent Anomaly Hits Quarantine Line} (Q)
        
        % Q to B
        (Q) edge[edge_fatal] node[lbl, above] {Hits Hard DoS Threshold, Physical Block} (B)
        
        % B loop
        (B) edge[->, ultra thick, draw=black!80, loop right, looseness=5] node[lbl, right] {Revoke TEE Certificate\\Permanent Handshake Terminated} (B);
        
    \end{tikzpicture}
\end{document}
